import { StyleSheet } from 'react-native';
import { colors, typography } from '@/src/theme';

// TODO: Implementar estilos do card do instrutor
// Este componente será desenvolvido posteriormente com mais complexidade

export const styles = StyleSheet.create({
  container: {
    width: '100%',
  },
  sectionTitle: {
    fontSize: typography.fontSize.lg,
    fontWeight: typography.fontWeight.medium,
    color: colors.secondary,
    marginBottom: 12,
  },
  // TODO: Adicionar estilos do card do instrutor aqui
});
