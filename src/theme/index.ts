// Exportar tudo do tema em um só lugar
export { colors } from './colors';
export { typography, textStyles } from './typography';
export { cardShadow, cardBorder, cardStyle, footerShadow } from './shadows';

// Também pode importar assim:
// import { colors, typography, textStyles, cardStyle, footerShadow } from '@/src/theme';
